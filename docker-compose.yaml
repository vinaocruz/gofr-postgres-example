version: '3'

services:
  app:
    build: .
    image: my-app
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
  db:
    image: postgres
    volumes:
      - ./.data/postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: example